/**
 * This is a template for an empty agent. This is all you need to create an agent.
 * Granted, it's not doing anything, but still, it can be loaded into EveJS.
 */


var agent = {RPCfunctions: {}};

// mandatory init function
agent.init = function(Eve) {

}









agent.exports = proxyAgent;